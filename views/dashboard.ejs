<%- include('partials/header') %>

<section class="hero">
  <div class="logo">🐺</div>

  <p class="subtitle">TON AVENTURE À 100 %</p>

  <h1><%= title %></h1>

  <p class="description">
    Bienvenue <strong><%= playerName %></strong>, chasseur elfe de la nuit.
  </p>

  <article class="progress-card">
    <span>Progression générale</span>

    <strong><%= progress %> %</strong>

    <div class="progress-bar">
      <div style="width: <%= progress %>%"></div>
    </div>
  </article>
</section>

<%- include('partials/footer') %>