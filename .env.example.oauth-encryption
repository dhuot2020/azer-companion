# Cle de chiffrement des tokens OAuth.
# Generer UNE FOIS et conserver dans .env / secret manager.
#
# Commande:
# node -e "console.log(require('crypto').randomBytes(32).toString('base64'))"
#
OAUTH_ENCRYPTION_KEY=

# Locale Blizzard API
BATTLENET_LOCALE=fr_FR
